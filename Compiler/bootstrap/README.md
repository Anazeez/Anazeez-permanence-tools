# Bootstrap

Bootstrap utilities establish new repositories, subsystems, and governed toolchain structures.

They must produce reviewable outputs and must not commit, push, deploy, or ratify automatically.
